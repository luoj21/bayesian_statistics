# Bayesian Statistics In Python

Some practical usages of Bayesian statistics in Python. The courses I've taken utilizing Bayesian statistics typically use ```R```

### Setup
- Download the data sets (links in each notebook)
- Create a virtual environment ```python -m venv .venv``` and activate with ```source .venv/Scripts/activate```
- Install dependencies ```pip install -r requirements.txt```